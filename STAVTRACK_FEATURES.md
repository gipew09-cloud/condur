# Что умеет Ставтрэк — и что из этого забираем себе

Составлено 21.08.2026 по скриншотам кабинета `stavtrack.online`, выгруженному
путевому листу и логам ретрансляции. Это не «список хотелок», а инвентарь: что
у поставщика уже есть, что из этого нам нужно, что мы можем сделать лучше.

---

## 1. Поток данных: что реально приходит в каждом пакете

Из лога приёмника (`app.telemetry.egts_receiver`, 21.08.2026):

```
params={'motion': 0, 'power': 27.98, 'ignition': 1, 'battery': 4.23,
        'fuel2': 65535, 'fuel3': 65535, 'fuel4': 65535, 'fuel5': 65535,
        'fuelTemp2': -128, ..., 'numSensorCanal0': 0,
        'temp1': -327.68, 'temp2': -327.68, 'acp1': 330,
        'totalDistance': 1692691177.96, 'lastSatTime': 1787324183,
        'speedLimit': 0.0}
```

| параметр | что это | статус у нас |
|---|---|---|
| `power` | напряжение бортсети, В | ✅ **храним** (`vehicle_states.voltage`), видно в карточке |
| `battery` | своя батарея трекера, В | ✅ **храним** — по ней видно обесточку |
| `ignition` | сырой бит зажигания | ✅ храним; двигатель определяем по `power` (бит залипает) |
| `motion` | признак движения | берём скорость, этого достаточно |
| `totalDistance` | одометр прибора, метры | ✅ храним (`mileage_km`) |
| `speedLimit` | лимит скорости на дороге | ❌ не берём — приходит 0 |
| `fuel2…5`, `fuelTemp2…5` | ДУТ и температура в баках | ❌ **датчиков нет**: 65535 / −128 = «не подключено» |
| `temp1`, `temp2` | температурные датчики | ❌ нет: −327.68 = «не подключено» |
| `acp1`, `numSensorCanal0` | аналоговые входы | не расшифрованы |
| `lastSatTime` | время последней связи со спутником | кандидат: отличать «нет GPS» от «нет GSM» |

**Вывод про рефрижератор:** температуру кузова Ставтрэк отдать не может —
датчика физически нет. Появится только со своим железом (Эскорт TD-BLE).

### Темп поступления данных (измерено по логу за час)

| машина | точек | интервал между точками |
|---|---|---|
| Т557ОС178 (ехала) | 112 | медиана **30 с** (минимум 1 с) |
| Т772НХ178 (стояла) | 62 | 60 с |
| У774ЕТ178 (стояла) | 27 | 60 с |

**Задержка «машина прислала → у нас в базе»: медиана 37 секунд** (10-й
процентиль 22 с, 90-й — 41 с). Это буферизация на стороне Ставтрэка: он
копит точки и отдаёт пачкой. Наш парсер обрабатывает мгновенно.

👉 Поэтому опрос кабинета переведён с 30 на **15 секунд** — быстрее смысла нет,
свежее ретрансляции метка всё равно не станет. Чтобы получить настоящее
«секунда в секунду», надо принимать поток от трекера напрямую, минуя Ставтрэк.

---

## 2. Экран мониторинга

| у Ставтрэка | у нас |
|---|---|
| Вкладки **Объекты / Треки / Геозоны** | Объекты ✅; треки ✅ (кнопкой из карточки); геозоны — отложено владельцем |
| Поиск и сортировка объектов | ✅ |
| Два вида списка: строки / плитки | ❌ не делали |
| Карточка: адрес обратным геокодированием + координаты | ✅ **сделано** — геокодер Яндекса, адрес + координаты под ним |
| Карточка: одометр, число спутников | одометр храним, на карте не показываем |
| Карточка: датчики (напряжение, зажигание, % заряда АКБ) | ✅ напряжение и обесточка |
| Картинка машины по категории и цвету | ✅ **сделано** — рисунок в метке, в строке списка и в карточке, цвет выбирается вручную на /vehicles |
| Кнопки быстрых действий на строке объекта | ❌ |
| **Чего у Ставтрэка нет, а у нас есть** | состояние в обводке метки, живой счётчик свежести точки, честный пунктир на разрыве связи, привязка к смене и рейсу, «требуют внимания» |

---

## 3. Треки

Ставтрэк: выбор объекта, период + «точный интервал», толщина линии (6 px),
**цветовой режим: по поездкам / однотонный / по скорости** (палитра 17 цветов),
слой «дорожные события» (старт, стоянка, парковка, превышение, …).

У нас: линия ехал / точка стоянки с длительностью / **пунктир на разрыве связи**,
линия идёт **градиентом** от бледного `#93c5fd` в начале пути к насыщенному
`#1d4ed8` у машины (как в макете редизайна: «хвост» с прозрачным дальним концом
плюс широкое свечение под линией) и прорисовывается от начала к машине.

Пунктира у Ставтрэка нет — он соединяет разрыв прямой, из-за чего рисует
«полёт через водохранилище». Это наше преимущество, терять нельзя.

**Стоит забрать:** раскраску трека по скорости (видно, где машина тошнила),
разбивку трека на поездки.

---

## 4. Автопарк → карточка устройства

### Основное
Имя, тип объекта (`fleetguide`, порт 7254), компания, **категория** (напр.
«Среднетоннажный грузовик») и **цвет** (белый / чёрный / жёлтый / оранжевый /
красный / синий / зелёный). Из пары «категория + цвет» рисуется картинка машины.

✅ **Сделано.** Колонка `vehicles.color` (миграция `0018`), та же палитра из семи
цветов, выбор в формах добавления и правки машины. Рисунок один
(`static/truck-reefer.png` — рендер владельца, белый, фон и тень убраны),
цвета получаются перекраской в canvas прямо в браузере: отдельного файла на
каждый цвет нет.

⚠️ Красится **только кузов** — пиксели ярче 0.62 по яркости. Резина, рама,
стёкла и решётка холодильной установки темнее порога и остаются как нарисованы.
Первая версия красила всё подряд, и синий грузовик выходил с синими колёсами и
синей тенью. Поэтому исходник обязан быть **светлым** рендером с тёмными
деталями: на чёрном рендере отделить кузов от резины нечем. Тест
`test_truck_artwork_has_dark_parts_to_protect` ловит подмену рисунка.

### Дополнительно
* расход по нормам, максимальный интервал между сообщениями, суточная норма моточасов;
* превышение скорости: «дорожные ограничения» или «фиксированное ограничение» + минимальное время превышения;
* **фильтрация валидности сообщений**: пропускать невалидные, минимум спутников (2), максимум HDOP (4), максимальная скорость (180).

👉 Фильтр валидности — прямой ответ на подмену координат у Пулково. У нас есть
флаг `is_valid`, но порогов по спутникам и HDOP нет. **Стоит сделать.**

### Команды (AT-команды терминалу, канал AUTO / TCP / UDP / VIRTUAL)
Очистка чёрного ящика, обновление ПО, **блокировка и разблокировка стартера
через реле**, номер ICCID, подключение ДУТ, холодный рестарт GPS-модуля,
перезагрузка терминала, запрос IMEI и силы GSM, зуммер по превышению скорости,
включение Bluetooth, роуминг, запрос ID трекера.

👉 Нам это не нужно и не будет доступно: команды идут в трекер, а мы получаем
только ретрансляцию. Появится со своим железом.

### Датчики
Типы: напряжения, зажигания, произвольный, топлива, агрегата, пробега,
абсолютного расхода топлива, оборотов двигателя, температуры, назначение
водителя. У датчика: имя, параметр из потока (`power`, `ignition`, `battery`,
`fuel2`, `motion`, `sat`, …), единица измерения, валидатор, **тарировочная
таблица** (X / a / b либо пары XY, с генерацией и границами).

👉 Тарировка нужна для ДУТ: литры считаются из напряжения по таблице. Пригодится,
когда поставим датчик топлива.

### Характеристики
Марка, модель, тип топлива (бензин / дизель / газ), марка топлива.

---

## 5. Путевой лист — умеет генерировать (проверено выгрузкой)

Формы: **№3 (легковой)** и **№4-С (грузовой)**, отчётная дата, серия и номер,
ставки для расчёта зарплаты (за километр и за час), галочки «заполнять данные
о поездках» и «использовать данные одометра». Выгружается в xlsx.

Что заполняется автоматически на обороте формы:

| колонка | пример из выгрузки |
|---|---|
| Место отправления / назначения | адреса обратным геокодированием: «60 к24 Софийская улица, Обухово…» → «5А Шушары, Санкт-Петербург» |
| Дата, время выезда и возвращения | 19.08, 14:06 → 14:43 |
| Пройдено, км | 12.41 |
| Итог за смену | всего в наряде 1:20, пройдено 21.52 км |
| Расчёт ЗП | за километраж 215.20 ₽, за часы 1.34 ₽, итого 216.54 ₽ |

👉 **Это близко к тому, что мы уже делаем актами.** У нас есть рейсы, смены,
схемы оплаты водителя и реквизиты — путевой лист по форме №4-С мы соберём
точнее, потому что знаем груз, заказчика и РЦ, а Ставтрэк знает только координаты.
Кандидат в отдельный блок.

---

## 6. Порядок, в котором это брать

1. ✅ ~~Ручной цвет машины~~ — сделано 21.08.
2. ✅ ~~Адрес метки обратным геокодированием~~ — сделано 21.08.
3. **Фильтр валидности точек**: минимум спутников, максимум HDOP.
4. **Раскраска трека по скорости**.
5. **Путевой лист №4-С** из наших рейсов и смен.
6. Плиточный вид списка объектов, быстрые действия в строке — по остаточному принципу.

Чего **не** берём: AT-команды терминалу (нет канала), температура и топливо
(нет датчиков), «дорожные события» Ставтрэка (у нас своя сегментация точнее).
