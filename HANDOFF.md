# condur — статус проекта и план на следующий чат

Дата среза: 2026-06-25. Это передаточный документ: что **уже сделано**, что **осталось**,
и где **известные проблемы**. Деплой на Railway, миграции через Alembic (сейчас до `0009`).
Стек не меняем. Бюджета на платные API/ключи нет. Git коммитит владелец сам.

---

## ✅ ЧТО УЖЕ СДЕЛАНО

### Сессия 2026-07-06 (правки по фидбеку владельца)
- **Геопозиция водителя заморожена**: `feature_cargo_geolocation=False` по
  умолчанию (GPS Stavtrack достаточно). Водитель больше не видит шаг «Отправить
  геопозицию» при «Выехал»/«Сдал груз».
- **Убрана кнопка «➕ Добавить смену»** у водителя (в `driver_no_shift_kb`):
  осталась только «➕ Добавить рейс», она сама создаёт смену. Хендлер
  `btn_add_shift` жив, но недостижим с клавиатуры.
- **Починен баг залипания на «📄 Документ»**: раньше в состоянии
  `UploadWaybill.waiting_for_photo` любое НЕ-фото (в т.ч. нажатие «Сдал груз»)
  ловилось ответом «Нужно именно фото» — водитель залипал. Теперь: (1) шаг
  необязательный — фильтр `~F.text.in_(kb.ALL_DRIVER_BUTTONS)` выпускает нажатия
  кнопок меню к их обработчикам (они сами чистят state через `state.clear()`);
  (2) случайный текст мягко отменяет шаг («Ок, без документа — продолжаем»).
  Добавлен набор `ALL_DRIVER_BUTTONS` в keyboards.py.
- **Удаление документов и чеков на сайте**: новые POST-роуты
  `/trip-doc/{id}/delete` (документ рейса от владельца),
  `/trips/{id}/waybill/delete` (фото ТТН от водителя → обнуляем file_id),
  `/expenses/{id}/receipt/delete` (чек расхода — чистим оба поля, сам расход
  остаётся). Кнопки «🗑 Удалить» с confirm в trip_detail.html и expense_edit.html
  (чек — через `formaction`/`formnovalidate`, чтобы не вкладывать формы).

### Сессия 2026-07-06 (продолжение): закреплённая машина — старт без выбора + напоминание
- **Фича A — старт смены без выбора машины.** Если у водителя задана «обычная
  машина» (`drivers.default_vehicle_id`) и она свободна — при «Начать смену»
  бот НЕ показывает список и НЕ добавляет никаких кнопок (`_begin_shift_on_default`
  в driver_bot): просто просит фото одометра для этой машины. Владелец явно
  просил чистый старт — привязал машину = никакого «выбрать/сменить/отменить».
  (Кнопку «🔁 Другая машина» убрали по его просьбе 2026-07-06 вместе с
  `change_vehicle_keyboard`/`cb_shift_change_vehicle`.) Если обычная машина
  занята другим водителем — предупреждение + выбор свободных. У кого «обычная
  машина» НЕ задана — прежнее поведение (выбор из списка / автостарт единственной,
  с анти-миссклик переспросом).
- **Фича B — напоминание водителю «поехал — начни смену».** В
  `vehicle_mixup_detector_job` (каждые 10 мин) добавлено: если закреплённая за
  водителем машина едет без открытой смены — водителю личное напоминание
  (`DRIVER_START_SHIFT_REMINDER`). Антиспам (важно!):
  (1) машина едет уже ≥ 3 мин (`telemetry_service.steady_moving_vehicle_ids` —
  фильтр от кратких скачков GPS); (2) дедуп не чаще раза в 2 ч на машину через
  events (`start_shift_reminder`); (3) не шлём, если у водителя уже есть
  активная смена (на любой машине); (4) свежесть GPS и is_valid уже
  фильтруются. Джобе теперь передаётся и `driver_bot` (main.py).
- Владельцу «едет без смены» приходит как раньше (отдельный дедуп) — не путать
  с напоминанием водителю.
- Тесты: 83 passed (новый: `steady_moving_filters_gps_glitches`).

### Сессия 2026-07-06: честный учёт зажигания (после разбора EGTS другим разработчиком)
- **Контекст**: другой разработчик установил, что датчик зажигания в
  ретрансляции Stavtrack (EGTS) сейчас НЕ приходит. В пакетах есть
  SR_POS_DATA=16 и SR_EXT_POS_DATA=17, но 17 — это точность/спутники, НЕ
  зажигание. Парсер честно даёт `ignition` только True или None, никогда False
  (см. `egts.py` PositionData.ignition; тесты `test_egts_parser.py`). Также
  добавлен разбор SR_STATE_DATA=20 (напряжения борта) и диагностическое
  логирование (power, ext_pos, unknown_sr, hex) — чтобы после включения
  датчиков в Stavtrack поймать новый тип подзаписи в логах egts-receiver.
- **Найден и исправлен баг моего вчерашнего кода**: функция «мотор заглушен»
  считала время по `ignition is False`, а такого значения в данных НЕТ →
  показывала бы «0 мин заглушен», подразумевая «мотор работал всю стоянку» —
  ложь. Фикс: `engine_off_minutes_from_points` возвращает **None** («нет
  данных»), пока в точках нет ни одного `ignition=False`. Когда датчик включат
  в Stavtrack и пойдут реальные False — сама начнёт считать. Пока: в боте и в
  статистике вместо мотора «—».
- Сообщение «приехал на РЦ» больше НЕ утверждает «двигатель заглушен» при
  stopped (мы этого не знаем). «двигатель работает» оставлено только для
  idle_engine (там зажигание точно пришло). Общее время стоянки и биллинг
  простоя честные — они по GPS-позиции, а не по мотору.
- Тесты: 82 passed (новый: `engine_off_minutes_none_when_sensor_absent`).
- ⚠️ **Датчики (что нужно от владельца)**: в Stavtrack включить в ретрансляции
  передачу датчиков — зажигание, питание, ДУТ/топливо, двери. После включения
  смотреть логи egts-receiver (появится новый тип подзаписи / STATE_DATA),
  дописать точный парсер и тогда ignition станет True/False по-настоящему.

### Сессия 2026-07-05 (продолжение): мотор в «уехал с РЦ» + вкладка «Статистика»
- **«Уехал с РЦ» показывает две цифры**: «Стоял там: 4 ч 20 мин» (от фактической
  остановки) + «Из них с заглушенным двигателем: ~3 ч 50 мин». Считается по
  точкам телеметрии (`telemetry_service.engine_off_minutes` →
  `sum_engine_off_seconds`: интервал приписывается состоянию первой точки,
  дыры >10 мин не считаются). Пишется в payload `rc_departed`
  (`engine_off_minutes`) — используется статистикой.
- **Вкладка «Статистика»** (`GET /stats`, пункт в топбаре): фильтр периода
  (дефолт — текущий месяц), KPI (приездов/суммарный/средний простой),
  **журнал простоев** (приехал/уехал/машина/водитель/РЦ/стоял/мотор заглушен —
  основа для счетов за простой), сводка по РЦ (где дольше держат), по водителям
  (рейсы/км по одометру смен/простой; водитель к простою привязывается по
  смене, активной в момент стоянки), заказы по неделям (рейсов + выручка).
  Источник простоев — события `rc_departed`. Правка записей и Excel-выгрузка —
  следующий шаг (в промте).
- Тесты: 73 passed (sum_engine_off_seconds, рендер stats.html).

### Сессия 2026-07-05: фикс ложных срабатываний геозон (приоритетный баг владельца)
- **Баг**: дороги (Московское шоссе) идут вплотную к РЦ — машина, проезжающая
  мимо, попадала в круг 400 м → ложные «приехал/уехал» (скрины владельца).
- **Фикс**: «приехал» засчитывается ТОЛЬКО если машина остановилась в зоне и
  стоит ≥ 4 мин (`telemetry_service.parked_long_enough`, отсчёт от
  `motion_since_at`; stopped ИЛИ idle_engine). Проезд мимо и светофор — не
  события. Цена: «приехал» приходит через ~4–9 мин после остановки.
- «Стоял там: N» теперь честный — от ФАКТИЧЕСКОЙ остановки (parked_since в
  payload rc_arrived), а не от срабатывания детектора. В сообщении «приехал»
  видно состояние мотора (работает/заглушен — эвристика зажигания из EGTS).
- **Миграция 0015**: `distribution_centers.geofence_radius_m` — свой радиус на
  РЦ (большие склады 400+ м: ставить 500–800). Поле «Радиус, м» на /routes.
  Выходной радиус = входной × 1.5. Пусто = 400 м.
- Кнопка геокодинга: если у всех РЦ уже есть координаты — явный баннер «нечего
  делать» (владелец принял это за «не работает»); убран двойной сабмит.
- `NEXT_SESSION_PROMPT.md` переписан начисто — большой промт для следующего
  разработчика: контекст, проблемы, риски, человеческий фактор, нагрузка,
  приоритеты (журнал простоев для счетов за простой, вкладка «Статистика»,
  GPS-км на смену (0016), связка маршрутов с фактическими РЦ, полигоны).
- Тесты: 71 passed (новые: parked_long_enough — мимо/светофор/стоянка).

### Сессия 2026-07-04 (поздний вечер): геозоны РЦ
- **Геокодинг РЦ**: кнопка «📍 Определить координаты по адресам» на `/routes`
  (`POST /routes/rc/geocode`) — бесплатный Nominatim/OpenStreetMap, без ключей;
  до 20 адресов за клик (пауза 1.1 с — политика сервиса), баннер с итогами
  (найдено/не найдено/осталось). Сервис: `app/services/geocode_service.py`
  (aiohttp лениво — чистый `parse_nominatim_response` тестируется локально).
  В Ставтрэке ничего настраивать НЕ нужно — геозоны наши.
- **РЦ на карте**: `/api/drivers-locations` теперь отдаёт `rcs`, на `/map`
  РЦ — синие точки с балуном (проверить глазами, что геокодер попал).
- **Детектор геозон** (`rc_geofence_job`, каждые 5 мин): вход в радиус 400 м →
  «🏬 {номер} ({водитель}) приехал на РЦ {имя}», выход за 600 м (гистерезис) →
  «🏁 уехал… Был там: N мин». Состояние — в `events` (`rc_arrived`/`rc_departed`,
  payload vehicle_id+rc_id+waited_minutes), переживает рестарты. GPS старше
  30 мин игнорируется. `rc_service.haversine_m` — расстояние по прямой.
- Тесты: 70 passed (`tests/test_rc_geofence.py`: гаверсинус, разбор Nominatim,
  гистерезис через ast).
- Владельцу: импортировать список РЦ на `/routes` (если ещё не), нажать
  «Определить координаты», проверить точки на карте, поправить промахи
  (широта/долгота в карточке РЦ). Дальше события пойдут сами.
- **Доработка по замечаниям владельца** (OSM может промахнуться; РЦ стоят кучно):
  (1) на `/routes` панель «🗺 Проверить и поправить точки на карте» — Яндекс-карта,
  красные метки РЦ **перетаскиваются мышкой** (dragend пишет в поля строки,
  строка подсвечивается — нажать «Сохранить»); точку без координат ставят
  кликом (выбор РЦ в селекте). Есть поиск по карте (searchControl).
  (2) Детектор геозон: машина привязывается только к **ближайшему** РЦ в
  радиусе, и пока «внутри» одного — соседние игнорируются (нельзя быть на
  двух складах сразу). Геокодер — черновик, финальные точки ставит владелец.

### Сессия 2026-07-04 (вечер): карточка машины, админ-уведомления, анти-миссклик
- **Карточка машины в боте владельца** (`vehicle:view:` в `owner_bot.py`): номер, марка,
  тип, норма л/100км, Stavtrack ID, сроки ОСАГО/ТО/тахографа (⚠️/⛔), GPS-статус сейчас
  (едет/стоит + с какого времени, свежесть 30 мин), пробег за сегодня по трекеру, кто в
  смене. Read-only, кнопка «Обновить». Редактирование — на сайте.
- **Уведомления владельца дублируются админам** (`notifications.py`): текст и фото уходят
  всем из таблицы `admins` (ошибки по адресату молча пропускаются). Миграция `0013`:
  `admins.notifications_enabled` + кнопка вкл/выкл на `/requisites`. `/login`-код — только
  тому, кто просил (не тронут). Бонус: `_get_owner` в боте владельца теперь узнаёт админа
  → со второго телефона работают меню и кнопки «Одобрить» и т.п.
- **Анти-миссклик машины**: миграция `0014` `drivers.default_vehicle_id` («обычная машина»,
  селект в карточке водителя на сайте). При старте смены на другой машине бот переспрашивает
  («Обычно вы ездите на X. Точно берёте Y?»), владельцу — уведомление.
- **GPS-детектор «поехала не та машина»** (`vehicle_mixup_detector_job`, каждые 10 мин):
  машина смены стоит >15 мин, а машина без смены едет → алерт владельцу; без такой пары —
  просто «едет без смены». Дедуп через `events` (2 ч на пару машин).
- **Исправление миссклика на сайте**: на страницах смены и рейса форма «✏️ Исправить
  водителя/машину» (`POST /shifts/{id}/reassign`, `/trips/{id}/reassign`). У смены рейсы
  обновляются каскадно, пометка в `events` (`shift_reassigned`/`trip_reassigned`). Для
  активной смены занятые водитель/машина не назначаются.
- **Водитель больше НЕ видит заработок** (решение владельца): `/balance` удалён,
  зарплата в конце смены удалена (был флаг — теперь совсем), убран из `/help` водителя.
  Владельцу всё приходит как раньше.
- **Тарифы спрятаны** (биллинг заморожен): `/tariffs` отвечает «не подключены», убраны
  упоминания из `/help` владельца и онбординга.
- Тесты: 62 passed (head миграций в `test_migrations.py` = `0014_driver_default_vehicle`).
- Ожидаем от владельца скриншоты багов «при добавлении второго водителя».
  Заметка: если открыть invite-ссылку с телефона, где уже привязан другой водитель,
  бот скажет «С возвращением» и ссылка не сработает — тестировать с разных аккаунтов.

### Бот водителя — упрощение через фича-флаги (всё в `app/config.py`)
- 10 флагов `FEATURE_*` (по умолчанию выключены, кроме `RECEIPT_OCR`, `ODOMETER_PHOTO`,
  `CARGO_GEOLOCATION`). Выключено = водитель не видит кнопку/шаг; включишь = появится.
  Документировано в `README.md`.
- Минимум кнопок: Начать/Конец смены, Новый рейс, Выехал, Сдал груз, SOS, Документ, Расход,
  «➕ Добавить смену/рейс» (оффлайн задним числом). «Статус» — только команда `/status`.
- Одна свободная машина → смена стартует сразу без выбора.

### Бот — ключевые потоки
- **Одометр:** водитель шлёт ТОЛЬКО фото (старт и конец); число км вписывает ВЛАДЕЛЕЦ по
  кнопке «📍 Указать пробег». Есть валидация (≤7 цифр, конец ≥ начала, ≤5000 км/смена) и
  одноразовость (повторно не вписать). Зарплата пересчитывается после ввода км.
- **Геопозиция** запрашивается на «Выехал» И «Сдал груз» (с кнопкой «Пропустить»).
- **Расход** доступен в любой момент, даже без открытой смены (`shift_id=None`).
- **Выручка рейса:** водитель указывает после сдачи груза («правило первого» — кто первый
  указал, тот зафиксировал; водитель НЕ перетирает уже указанное, владелец — главный,
  одобряет/меняет; все кнопки одноразовые). Двойного ввода больше нет.
- **Зарплата:** типы per_km / per_trip / percent / fixed_per_shift / **fixed_per_month** (оклад).
- Владелец: одобрить/изменить/отклонить расход; установка часового пояса (`/timezone`).

### Веб-кабинет — дизайн «Свежий путь»
- Базовый рескин на ВСЕХ страницах: шрифт Onest, палитра, белые карточки, таблицы-в-карточках,
  статус-плашки (фильтр `pillclass`/`statusru`), единые заголовки `.page-head`.
- **Дашборд** — собран 1:1 по макету (KPI, «Требуют внимания», график выручка/прибыль,
  пончик расходов, «машины в работе», превью карты).
- KPI-карточки сверху списков: Рейсы / Машины / Водители / Расходы / Финансы.
- Вход (`login.html`) оформлен карточкой с брендом.

### Веб-кабинет — функции
- Добавление и редактирование рейсов на сайте; правка выручки (прибыль пересчитывается).
- Прибыль рейса = выручка − топливо − прочие расходы (привязка расходов к рейсу).
- Правка расходов на сайте (сумма/категория/статус) + загрузка фото чека владельцем
  (хранится в Postgres, миграция `0004`).
- Документы по рейсу: просмотр ТТН + загрузка владельцем (Postgres, миграция `0003`).
- Примерное время в пути по маршруту (из истории).
- **Статистика по машине** (только владельцу): `GET /vehicles/{id}/stats` — пробег/рейсы/
  выручка/расходы/прибыль/маржа за месяц и за всё время + документы.
- Пагинация списка рейсов; русские статусы везде.

### Excel
- Экспорт финансов `/finances/export.xlsx`.
- **Акт 101 РС (настоящая форма)** `/acts` + `/acts/export.xlsx` (`act_service.build_act_101rs`):
  один лист со всеми рейсами за период (1, 2, 3…), шапка с реквизитами **Исполнителя**
  (с `owner`) и **Заказчика** (таблица `customers`), Основание (Договор), колонки
  № · Наименование · Кол-во · Ед. · Цена · Сумма, ИТОГО, сумма прописью, «Без налога (НДС)»,
  подписи сторон. Воспроизводит образец «Рузисеть ИП Кибиткина № 101 РС». Реквизиты
  редактируются на **`/requisites`** (Исполнитель + заказчики). Миграция `0006`.
  - Проверено: генератор на данных образца даёт ИТОГО 292 000 ₽ и «Двести девяносто две
    тысячи рублей 00 копеек» — совпадает с оригиналом.
  - Старый `build_acts_workbook` (листы по РЦ) больше не используется — можно удалить при случае.
  - Доработки (2026-06-25, по фидбеку): (1) **переименован в «Акт сверки»** — параметр
    `title` у `build_act_101rs` (деф. «Акт»), на сайте поле «Название документа» (деф.
    «Акт сверки»), кнопка на Финансах «📄 Акт сверки»; (2) **выбор рейсов галочками** — на
    `/acts` чек-лист рейсов за период (выбрать все / по одному, живой счётчик), в Excel идут
    только отмеченные (`trip_ids` в `acts_export`, пусто = все); (3) **формат как у образца
    101**: шапка таблицы без синей заливки (чёрный жирный текст), деньги `#,##0.00` без ₽,
    шрифт 9 пт, чёрные тонкие рамки (свои константы `_ACT_*` в act_service).

### Планировщик / надёжность
- `notify_owner` устойчив к сетевым сбоям; per-owner try/except в джобах; дедуп «тишины»
  через Event; алерт о невыходе водителя (`no_show_detector_job`).

### Схема (Alembic): 0001 initial · 0002 manual flags · 0003 trip_documents ·
0004 expense web receipt · 0005 salary per month · **0006 requisites + customers** ·
**0007 admins** · **0008 distribution_centers** · **0009 vehicle_telemetry**.

### Багфиксы и фичи по фидбеку (2026-07-03)
- **Карточка водителя — за ВСЁ ВРЕМЯ** (было за месяц → 0 в новом месяце). `_drivers_stats`
  без фильтра по месяцу; подпись «за всё время». (Машины по-прежнему за месяц — так помечено.)
- **Бот, добавление водителя**: `cb_add_driver` проверяет владельца СРАЗУ (раньше падал
  `SOMETHING_WRONG` после ввода данных, если аккаунт не владелец).
- **Логин**: убран непонятный бренд «Свежий путь» → просто «Вход в кабинет».
- **Карточка машины**: номер+статус в один ряд, название/тип отдельной строкой (длинный
  номер больше не ломает вёрстку).
- **Админы кабинета (полный доступ)** — модель `Admin` + миграция `0007`. Владелец добавляет
  админа по Telegram ID на `/requisites`. Вход: админ шлёт `/login` боту владельца → код →
  веб `/login`. JWT теперь несёт `tid` (кто вошёл); `current_owner` проверяет, что доступ
  админа не отозван — **удаление админа действует сразу**, а не через 7 дней (TTL JWT).
  ⚠️ На Railway задать длинный случайный `JWT_SECRET` (дефолт «change-me-in-production»).
- Правка расхода на `/expenses/{id}` уже работала (форма сумма/категория/статус/фото +
  «Сохранить», даже после одобрения) — ничего не меняли.
- Кейс со `/start` от админа обработан: `cmd_start` при admin-telegram_id не запускает
  онбординг владельца, а подсказывает отправить `/login`.
- **Справочник РЦ — первая рабочая версия**: таблица `distribution_centers` + миграция
  `0008`; управление на `/routes`; импорт `.xlsx` с колонками «РЦ/Название», «Адрес»,
  опционально «Алиасы», «Широта», «Долгота»; мягкое скрытие записей. Акт сверки теперь
  подставляет канонический адрес РЦ, если `Trip.destination` совпал с названием, адресом
  или алиасом. Если совпадения нет — остаётся старый свободный текст.
- **Акт сверки, пустой выбор рейсов**: чек-лист больше не превращает снятые все галочки
  в «все рейсы»; кнопка скачивания блокируется при нулевом выборе, а backend различает
  прямую ссылку и выбор через чек-лист.
- **Багфикс справочника РЦ**: чистая логика вынесена в `app/services/rc_service.py`;
  импорт Excel и поиск адреса тестируются отдельно, координата `0` больше не теряется.
- **Stavtrack/EGTS — первый безопасный слой**: добавлен отдельный TCP-процесс
  `python -m app.telemetry.egts_receiver`, единый Railway-runner `python -m app.run`
  с переменной `SERVICE_ROLE=egts`, миграция `0009_vehicle_telemetry`, поле
  `vehicles.stavtrack_object_id`, таблицы `vehicle_telemetry_raw_packets`,
  `vehicle_telemetry_points`, `vehicle_states`. Подробная инструкция — `STAVTRACK_INTEGRATION.md`.
  - ⚠️ Приёмник этапа 1 сознательно без авторизации: любой, кто узнает адрес:порт
    TCP-прокси, может писать мусор в `vehicle_telemetry_raw_packets` (до 64 КБ/пакет),
    и таблица растёт бесконечно. При добавлении парсера EGTS: отбрасывать пакеты с
    неизвестным terminal_id и добавить джобу очистки старых сырых пакетов (7–30 дней).
- **Stavtrack/EGTS — этап 2: ПАРСЕР ГОТОВ (2026-07-03, вечер)**. Новый модуль
  `app/telemetry/egts.py` (чистый, без БД): транспортный заголовок + CRC8/CRC16,
  записи, подзапись EGTS_SR_POS_DATA (координаты, скорость, курс, пробег, зажигание
  из DIN bit0 или движения, VLD), `build_response()` — ACK EGTS_PT_RESPONSE.
  **CRC8 и OID проверены на реальном пакете из логов Railway (OID=129772 совпал
  с Stavtrack ID машины).** `egts_receiver` теперь: буферизует TCP (склейка/нарезка
  пакетов), сохраняет raw со статусом parsed/failed/ignored, пишет
  `vehicle_telemetry_points`, апсертит `vehicle_states`, **отвечает трекеру ACK**
  (без ACK Stavtrack рвал связь каждые ~2 сек — поэтому «ничего не происходило»).
  Привязка: OID == `vehicles.stavtrack_object_id`; неизвестный ID → ignored
  («unknown terminal id»), ID у нескольких машин разных владельцев → ignored
  («ambiguous terminal id»), никому не пишем. Карта `/map` показывает машины из
  `vehicle_states` (плашка 🚚 с номером; синяя = зажигание вкл, серая = выкл,
  полупрозрачная = точка старше 10 мин). Тесты: `tests/test_egts_parser.py`
  (roundtrip, полушария, CRC-повреждения, нарезка потока, ACK). Осталось из плана
  разработчика: сверка GPS↔рейсы, авто-смены по зажиганию, алерты (см. блок 4).
- **Гонка выручки закрыта (2026-07-03, вечер)**: водитель мог перетереть выручку
  владельца, если печатал сумму, пока владелец вводил свою. Теперь у водителя
  атомарный `set_trip_revenue_if_empty` (UPDATE … WHERE revenue_rub IS NULL) —
  «правило первого» держится даже при одновременном вводе; при отказе водителю
  показывается «Выручка уже указана владельцем: X ₽». Владелец, как и раньше,
  перетирает всегда (он главный).
- **Биллинг ЗАМОРОЖЕН (2026-07-03)**: `BILLING_ENABLED` (деф. off) в config;
  `can_add_vehicle` при выключенном биллинге не ограничивает число машин (лимит
  9999). Код тарифов/подписок не удалён — включается одной переменной, когда
  проект пойдёт в продажу. Причина: пока используется только для своей компании.
- Новый документ **`GPS_IDEAS_PROMPT.md`** — промт для команды/ИИ по генерации
  идей GPS-контроля (что уже есть, что возможно, вопросы для штурма).
- **GPS этап 2.1 — фиксы по реальной обкатке (2026-07-03, ночь)**:
  (1) зажигание: у Stavtrack датчик зажигания не в POS_DATA.DIN (реально DIN=0,
  MV=0 при 54 км/ч) → `PositionData.ignition` теперь учитывает и скорость > 0;
  (2) «машина в Африке»: недостоверные/нулевые точки (VLD=0 или ≈(0,0)) больше
  НЕ обновляют координаты `vehicle_states` — только last_seen/ignition/пометку;
  карта фильтрует «нулевой остров»; (3) карта: явный статус «🟢 едет · N км/ч /
  🟡 стоит, двигатель работает / ⚪️ стоит» вместо «зажигание вкл/выкл»;
  (4) `/trips`: KPI (рейсы/выручка/прибыль) перенесены в партиал `_trips_table`
  — при HTMX-фильтре по датам обновляются вместе с таблицей (раньше «не менялись»);
  (5) **сравнение пробега GPS↔одометр**: новый `app/services/telemetry_service.py`
  (пробег за период = max−min `mileage_km` счётчика трекера — устойчив к лагам
  GPS в городе); при вводе км владельцем бот дописывает «📡 По GPS: N км.
  Расхождение: ±X км», с ⚠️ при >10%. Фикс гонки выручки был уже в коде
  (set_trip_revenue_if_empty) — требовал только деплоя.
- **Яндекс.Карты вместо Leaflet + гео конца смены (2026-07-04)**:
  (1) `/map` полностью переписан на **Яндекс.Карты JS API 2.1** (Leaflet/OSM/CARTO
  удалены отовсюду, тест это сторожит). Ключ — ENV `YANDEX_MAPS_API_KEY`
  (config: `yandex_maps_api_key`); без ключа страница показывает подсказку, не
  падает. Свои HTML-плашки маркеров сохранены (номер на карте, цвет = статус:
  зелёный едет / оранжевый двигатель на стоянке / серый стоит, полупрозрачный =
  сигнал старше 10 мин). Балун: статус, «⏱ с 21:52 / со вчера, 21:52 / с 01.07,
  21:52 · уже N» (`smart_since_label` в timeutil), «В смене/Без смены»
  (`has_active_shift` в API), время сигнала в таймзоне владельца, пометки про
  недостоверный GPS и старый сигнал. Обновление каждые 30 с + мгновенно при
  возврате на вкладку (visibilitychange); камера подгоняется ОДИН раз — карта
  не прыгает. Дефолт-центр — СПб. Зум-контролы теперь яндексовские (старые
  «плюсики» с кривой вёрсткой ушли вместе с Leaflet).
  (2) **Гео при завершении смены**: после «Конец смены» бот просит геопозицию
  (кнопка/Пропустить, за флагом FEATURE_CARGO_GEOLOCATION); точка пишется в
  events (`context=shift_end`), владельцу уходит ссылка на **Яндекс.Карты**
  (`yandex.ru/maps/?pt=lon,lat`). Состояние `EndShiftLocation` в states.
  (3) **Зажигание/датчики**: парсер теперь читает подзапись EGTS_SR_STATE_DATA
  (тип 20: режим + напряжения питания, `TerminalState`); приёмник логирует
  напряжение борта, типы неизвестных подзаписей и полный hex пакета — по этим
  логам после деплоя калибруем настоящий датчик зажигания Stavtrack (поля
  напряжений однобайтовые 0.1В, максимум 25.5В — для 24В-сети грузовика может
  упираться в потолок, поэтому в ignition пока не заведено).
- **Постоянные сессии кабинета + «Устройства» (2026-07-04)**: вместо 7-дневного
  JWT — серверные сессии в таблице `web_sessions` (миграция `0012`): в cookie
  случайный токен (10 лет), в БД только его SHA-256. **Вход не слетает сам** —
  живёт, пока его не завершат. На «Реквизиты» новый блок **«Устройства»**:
  список активных входов (кто — владелец/имя админа, браузер·ОС из User-Agent,
  IP, время входа и последнего визита, метка «это устройство»), кнопки
  «Завершить»/«Выйти здесь» и у владельца — «Выйти на всех устройствах, кроме
  этого». Права: владелец гасит любые устройства, админ — только своё текущее
  (`_viewer_telegram_id` определяет зрителя и для session-cookie, и для
  переходного JWT). `/logout` гасит сессию в БД. Удаление админа гасит все его
  устройства мгновенно. Старые 7-дневные JWT принимаются как переходный
  fallback (никого не разлогинило при деплое) — доживут и истекут, после
  повторного входа всё уже постоянное. `current_owner` обновляет
  `last_seen_at` не чаще раза в 5 минут (без лишней записи в БД). Тесты:
  токен/хэш/device_label, рендер блока устройств для владельца и админа,
  head миграций обновлён на `0012`.
- **Финансы/фильтры/скрытый сброс (2026-07-04)**:
  (1) **Финансы**: график «Денежный поток» строится по выбранному периоду
  «с… по…» (`timeutil.cashflow_buckets`: ≤45 дней — по дням, ≤200 — по неделям
  «нед. дд.мм», дальше — по месяцам) + кнопки быстрых периодов
  Неделя/Месяц/3/6/12 мес (ставят даты и перезагружают страницу — KPI, график
  и «Прибыльность направлений» пересчитываются согласованно). Подписи месяцев
  теперь «июл 2026» (владелец читал «июл 26» как «26 июля»); формат поправлен
  и на дашборде. `_month_floor/_add_months/_RU_MON` переехали в timeutil
  (month_floor/add_months/RU_MONTHS_SHORT), в router — алиасы.
  (2) **Рейсы, «Применить не работает»**: селект «— все —» слал пустой
  `driver_id`, FastAPI отвечал 422, HTMX молча игнорировал. Теперь id
  парсится мягко (str→int), фильтр работает.
  (3) **Расходы**: новые фильтры — водитель, машина (через смену; расходы вне
  смены под фильтр машины не попадают — на странице подсказка), период дат,
  кнопки «Применить»/«Сбросить».
  (4) **Скрытая команда `/wipe`** в боте владельца (в /help НЕ упомянута,
  тест это сторожит): полный сброс тестовых данных владельца через
  `maintenance_service.wipe_owner_data` — рейсы, смены, водители, машины,
  расходы, документы, GPS-точки/состояния/пакеты, шаблоны маршрутов, ручные
  записи, сводки, справочник РЦ, заказчики. ОСТАЮТСЯ: аккаунт владельца,
  реквизиты Исполнителя, админы, веб-сессии (устройства), тариф. Подтверждение
  точной фразой «УДАЛИТЬ ВСЁ» (константа WIPE_CONFIRM_PHRASE), отмена —
  /cancel или любой другой текст. Хендлеры вставлены ПЕРЕД fallback-ловцом.
  Тесты: 59 passed (buckets день/неделя/месяц и границы 45/200, пресеты
  финансов, фильтры расходов, «wipe не в help» + согласованность фразы).
- **/wipe = полный ноль + защита БД от мусора (2026-07-04, вечер)**: по фидбеку
  владельца `/wipe` теперь удаляет ВСЁ, включая сам аккаунт: + админы, входы в
  кабинет (web_sessions), тариф и запись owners с реквизитами (последней).
  После сброса `/start` начинает регистрацию с нуля («Как называется ваша
  компания?»), в базе по пользователю не остаётся ни одной строки, кабинет
  разлогинивается. Тест: `delete(Owner)` идёт после `delete(WebSession)`.
  Защита от разрастания БД: (1) приёмник EGTS больше НЕ сохраняет пакеты
  неизвестных/непривязанных терминалов (после wipe чужой трекер 128464 сыпал
  ignored-пакеты в базу каждые секунды) — теперь только строка в лог
  «EGTS skipped» + ACK, ни одной записи в БД; (2) новая джоба
  `telemetry_cleanup_job` (ежесуточно 00:37 UTC): сырые пакеты старше 7 дней
  и GPS-точки старше 60 дней удаляются (константы RAW_PACKETS_KEEP_DAYS /
  TELEMETRY_POINTS_KEEP_DAYS в scheduler_jobs).
  Примечание по логам: `unknown_sr=[17]` — это EGTS_SR_EXT_POS_DATA
  (точность/спутники), не датчики; блока STATE_DATA (power=—) в пакетах этого
  трекера нет — датчик зажигания ждём с установкой датчиков (дверь/ДУТ придут
  отдельными подзаписями, приёмник их залогирует).
- **Ревизия + фикс импорта РЦ (2026-07-03, вечер)**: проверено всё сделанное выше —
  цепочка миграций 0001→0009 линейна, всё компилируется, 35 тестов зелёные. Найдено и
  исправлено: (1) реальный файл владельца «РЦ Адреса Спб.xlsx» (одна колонка, без шапки)
  импортировал 0 строк — теперь одноколоночные файлы импортируются (адрес = вся ячейка),
  тест `test_rc_xlsx_import_single_column_file`; после импорта скрыть на `/routes` 4
  служебные строки (Агропарк + 3 строки машина-водитель-прицеп). (2) На `/routes/rc/import`
  добавлен лимит размера файла 6 МБ. Замечание: короткий ввод «Магнит Колпино» не матчится
  с «РЦ Магнит (Тандер, в Колпино)…» — для таких случаев вписать алиасы на `/routes`.

### Исправленные баги
Skip-краш при добавлении машины · часовой пояс · «Изменить маршрут» · отрицательный пробег
одометра + валидация + одноразовость · двойной ввод выручки.

---

## ⛔️ ЧТО ОСТАЛОСЬ (приоритет сверху вниз)

### 1. ✅ Excel-акт 101 РС — СДЕЛАНО (настоящая форма)
Реквизиты Исполнителя/Заказчика, шапка, колонки формы, итог прописью, подписи — готово
(см. раздел Excel выше; страница `/requisites`, миграция `0006`). Остаточные нюансы:
- В описании строки акт берёт канонический адрес из справочника РЦ, если есть совпадение
  с `Trip.destination`; если справочник пустой или алиас не совпал — остаётся свободный
  текст, что ввёл водитель/владелец.
- В образце прицепа в описании НЕТ (только тягач + водитель), поэтому колонки «№ ТТН/прицеп»
  из старого плана не делали. Привязки «машина→водитель→полуприцеп» есть в `РЦ Адреса Спб.xlsx`
  (строки 20–22) — добавить позже, если заказчик попросит прицеп в акте.
- НО (УСН/ОСН) в образце не фигурирует — не вносили; добавить полем, если понадобится.

### 2. ✅ «Богатый» дизайн вкладок — СДЕЛАНО (2026-06-25)
Общий слой `.sp-*` вынесен из dashboard.html в `cabinet.css` (карточки, KPI, графики,
сетка карточек, рейтинг-полосы, пончик) — переиспользуется на всех вкладках. По вкладкам:
- **Финансы:** KPI (.sp-kpi) + график «Денежный поток · 6 месяцев» (доход/расход бары +
  прибыль линией, Chart.js, период `6m` добавлен в `_dashboard_chart`) + «Прибыльность
  направлений» (рейтинг маршрутов по прибыли рейсов, полосы).
- **Машины:** таблица → карточки `.sp-vcard` (P&L за месяц, пробег, статус «в рейсе/свободна»,
  ОСАГО). HTMX-правка сохранена (партиал `_vehicle_row.html` теперь div с тем же id).
- **Водители:** карточки с аватаром-инициалами, статусом (в смене/простой/свободен), ЗП,
  смены/рейсы/пробег/выручка. HTMX-правка сохранена.
- **Расходы:** KPI + пончик «структура по категориям» (Chart.js).
- **Рейсы:** KPI обновлён до `.sp-kpi` (таблица с фильтром/пагинацией оставлена — для списков
  уместнее карточек).
Примечание: вёрстка не пиксель-в-пиксель с бандлом `Свежий путь - дашборд.html` (он
нечитаемый экспорт), а на дизайн-системе дашборда (которая и была сделана 1:1 по макету) +
именованные виджеты из макета. Проверено компиляцией и рендером шаблонов (живьём — после деплоя).

⚠️ ВАЖНО про стили: при первом деплое стили `.sp-*` не применились — браузер держал старую
`cabinet.css` (кэш/не закоммичен). Поэтому стили `.sp-*` вынесены в партиал
`templates/_sp_styles.html` и встроены в `<head>` каждой вкладки через
`{% block head %}{% include "_sp_styles.html" %}{% endblock %}` — едут вместе с HTML, от кэша
CSS не зависят. (Копия тех же правил осталась и в `cabinet.css` — безвредно, инлайн перекрывает.)
После деплоя сделать hard-refresh (Cmd+Shift+R).

### 3. ✅ Справочник РЦ — ПЕРВАЯ ВЕРСИЯ СДЕЛАНА (2026-07-03)
Есть таблица РЦ, ручное управление и импорт `.xlsx` на `/routes`; акт использует
канонический адрес при совпадении пункта назначения с названием/адресом/алиасом. Осталось
наполнить реальными адресами из файла владельца и позже использовать координаты для геозон.

### 4. Stavtrack / EGTS — ПЕРВЫЙ ДИАГНОСТИЧЕСКИЙ СЛОЙ СДЕЛАН (2026-07-03)
Есть отдельный TCP-приёмник `app.telemetry.egts_receiver`. На Railway теперь запуск общий:
`python -m app.run`; для GPS-сервиса надо задать `SERVICE_ROLE=egts`. Он сохраняет сырые
пакеты в `vehicle_telemetry_raw_packets`; это нужно, чтобы подтвердить поток от Stavtrack и
снять реальные EGTS-данные. Следующие шаги: подключить 1 машину, проверить логи, затем
добавить ACK/парсер EGTS и уже после этого считать координаты, зажигание, подозрительные км
и движение без активного рейса. Инструкция для Railway/Stavtrack — `STAVTRACK_INTEGRATION.md`.

### 5. Прочее
- OCR чека — дормант (нет ключа/бюджета); включится, когда будет ключ.
- Отчёт по месячной зарплате: нет сводной ведомости (водителю зарплата в боте
  НЕ показывается — решение владельца от 2026-07-04).

---

## 🚫 СОЗНАТЕЛЬНО НЕ ДЕЛАЕМ (предложения «технического менеджера» — для другого масштаба)
Микросервисы/K8s/RabbitMQ, Telegram Mini Apps, Elasticsearch, Whisper/голосовой ИИ,
computer-vision, ЭДО (Диадок/СБИС), 1С, GPS-трекеры (Wialon), полный биллинг-CRM
(Contractor/Contract/Tariff/Act как enterprise). Причина: нет бюджета/ключей/команды, риск
сломать рабочий MVP. Вернуться при росте.

---

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ / ЗАМЕТКИ
- В боевом боте много **тестового мусора** (mmm, ттт, «оыраовраор») — почистить тестовые
  смены/рейсы/расходы, иначе акты и статистика выглядят несерьёзно.
- Локально вживую не запустить (нужны Postgres + токены). Но есть юнит-тесты:
  `tests/test_acts_auth_web.py` (26 тестов: генератор акта, сумма прописью, JWT/отзыв
  админа, рендер всех страниц) + `tests/test_feature_flags.py` +
  `tests/test_egts_receiver.py`. Запуск: `pytest`
  (нужен Python 3.11, как в проекте; для test_feature_flags — установленный aiogram).
  `act_service` и рендер шаблонов тестируются без БД.
- Адреса РЦ теперь можно хранить в базе через `/routes`; пока база будет пустой, акт
  продолжит использовать свободный текст `Trip.destination`.
