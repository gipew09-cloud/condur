# Передача сессии — Condur, 31.08.2026

Промт для новой сессии. Скопировать целиком в начало разговора.

---

## Кто я и что за проект

Владелец — **не программист**, ИП Кибиткина, Санкт-Петербург. Три рефрижератора
возят свежие продукты со складов в РЦ торговых сетей. Пишем **Condur** — свой
TMS-кабинет (FastAPI + Jinja2 + Pico + ванильный JS, Postgres, Railway) и
мобильное приложение (Flutter, Android).

**Как со мной работать** (проверено, не отступать):

- по-русски, коротко, **по одной задаче за раз**;
- точечные правки, не переписывать принятое;
- **не предлагать git push** — деплою сам, когда скажу;
- видео присылать могу, но **обрабатывать его нельзя** — нужны кадры;
- **не переписывать длинные идентификаторы со скриншотов глазами** (ключи,
  IMEI) — просить текстом;
- ⚠️ **функция должна быть везде.** Когда я прошу что-то, я имею в виду весь
  продукт: сделал в приложении — сделай и в кабинете. Иначе для меня это
  несделанная работа.

## ⚠️ Три вещи, которые я должен сделать сам (кодом не закрыть)

1. **Закрыть репозиторий сервера.** `gipew09-cloud/condur` **публичный** —
   проверено запросом без авторизации. Репозиторий приложения приватный,
   серверный нет.
2. **Отозвать ключ LlamaParse.** В `tests/test_receipt_text_ocr.py`, строки 245
   и 437, фикстурой вписано настоящее на вид значение (`llx-…zs1y`, `llx-…mJaJ`)
   — оно есть во всех 164 коммитах публичной истории. Удаление строки НЕ
   помогает, помогает только отзыв в кабинете LlamaParse.
3. **Задать `TELEMETRY_PASSWORD`.** В `egts_receiver.py:553`: пароль не задан →
   **пускаем всех**. Кто знает адрес и IMEI, может слать ложные координаты в
   систему, которая объявлена доказательством в споре с водителем.

Хорошая новость по той же проверке: в истории репозитория **нет** `.env`,
дампов БД, токенов ботов и паролей к базе.

## Состояние на конец сессии

**330 тестов сервера зелёные, 20 тестов приложения зелёные.** Сервер владелец
выкатывает сам; часть правок этой сессии ещё не на боевом.

### Что сделано за сессию

**Приложение (Flutter, репозиторий `gipew09-cloud/condur-app`, приватный)**

- Сборка вынесена на **GitHub Actions** (Мак больше не греется): Java 21,
  Flutter 3.47.1, тесты, APK в Releases. Два секрета: `YANDEX_MAPKIT_KEY` и
  `ANDROID_KEYSTORE_BASE64`.
- Постоянный ключ подписи (`android/app/condur-beta.jks`, в git его нет) —
  иначе каждая сборка имела бы свою подпись и обновления не вставали бы поверх.
- Иконка — знак Condur вместо заглушки Flutter; в шапке «Condur · Мониторинг».
- Машины рисуются **тем же рисунком, что в кабинете**, с той же перекраской
  (порог кузова 0.62); одна плитка на метку, список и карточку.
- Пунктир у «GPS недостоверный», метки уменьшены, адрес в карточке.
- Геозоны РЦ, **хвост** за выбранной машиной (15 мин, тает к концу), **стрелка
  направления** (курс считается по перемещению — сервер его не отдаёт).
- Понятные ошибки вместо текста исключения («Нет интернета» вместо
  `ClientException with SocketFailed host lookup`).

**Кабинет — вкладка «Треки» с проигрывателем**

- Произвольный период «с… по…», чипы Сегодня/Вчера/7 дней, список смен.
- Плеер: пауза, ×1 ×2 ×5 ×15 ×60, полоса с перемоткой, шаг **‹ ›** по одной
  точке, камера едет за машиной, стрелка направления на кружке.
- Приборы на текущей точке: зажигание, топливо, температура топлива.
- Карточка качества периода: GPS есть/частично/нет + список разрывов с
  причиной; одометр ok/partial/missing/reset_detected; «Показано не всё» при
  упоре в лимит 20 000 точек.
- События водителя на треке (38 типов пишутся в `events`, на карту выведен 21).

**Сервер**

- `/api/vehicles/{id}/track` — `from`/`to`, `times` по каждой точке,
  `fuel_litres`/`fuel_temp_c`/`ignition` по каждой точке, `quality`,
  `truncated`, `gps_path_distance_km`.
- `/api/vehicles/{id}/shifts` — смены за период, `odometer_distance_km`.
- `/api/vehicles/{id}/events` — действия водителя за период.
- Фоновое распознавание одометра (водитель больше не ждёт LlamaParse).
- Починка складов на «Маршрутах»: общий ключ `origin_key`.
- «Приложение Condur · Android» в списке устройств вместо «Браузер · Android».

## ⚠️ Ошибки, которые я допустил — и чему они учат

1. **«Плеер слишком быстрый» — четыре раза.** Я четыре раза уменьшал множитель
   (60 → 10 → 4 → 1), а причина была в другом: цель шага считалась от времени
   ТЕКУЩЕЙ точки, за кадр набегало 16 мс, и этого всегда хватало на шаг —
   плеер шёл 60 точек в секунду при любой скорости.
   **Урок: если жалоба повторяется после «починки» — чинится не то.**
2. **«Всё криво».** Панель списка была разложена сеткой ровно на четыре блока;
   я добавил вкладки — строка поиска уехала в чужую строку и сплющилась до
   11 пикселей. Нашлось только когда **отрендерил страницу и померил элементы в
   браузере**. По скриншотам гадать бесполезно.
3. **Цвет текста плавающих панелей.** `.mon-play` не задавала `color` и
   наследовала серый от страницы: в тёмной теме заголовок смены читался с
   трудом. Нашлось тоже только глазами.
4. **Часовой пояс.** Поля периода читались как UTC вместо пояса владельца —
   период уезжал на три часа.
5. **Километры** делились поровну на число точек, поэтому счётчик рос у стоящей
   машины. Теперь по реальному расстоянию.

## Как проверять работу (без боевого доступа)

1. `pytest -q` — 330 тестов.
2. Отрендерить шаблон и проверить синтаксис скрипта:
   `_render("map.html", …)` из `tests/test_acts_auth_web`, вытащить `<script>`,
   прогнать `node --check`.
3. **Для вёрстки и цвета — открыть страницу в браузере и измерить элементы
   через JS.** Положить отрендеренный файл в папку проекта, открыть, читать
   `getBoundingClientRect` и `getComputedStyle`. Только так нашлись три из пяти
   ошибок выше.
4. Кэш статики руками чистить не нужно: метка версии считается по коммиту
   деплоя (`_asset_version`), ручных `?v=4` в проекте быть не должно.

## Что говорили другие нейросети — и что мы из этого взяли

Владелец приносил шесть разборов. Полезного там было четыре вещи, остальное —
дорожные карты на двадцать пунктов и функции из Geotab, у которого тысячи машин.

### ✅ Взяли

- Обязательный пароль телеметрии; срок жизни сессии (сейчас **10 лет**,
  `auth_service.py:89`) хранить в БД, а не только в cookie.
- Составной индекс `(vehicle_id, observed_at)` — его нет, а запрос трека всегда
  идёт по обоим полям.
- **Разные имена в API** для GPS-пути и одометра — сделано.
- Таксономия причин отсутствия данных: «задержана доставка» ≠ «машина стояла».

### ❌ Отвергли

Пятиминутные корзины агрегатов, таблица разрывов, repair job, backfill,
`quality_version`, Redis-кэш, партиционирование, RLS, отдельный сервис приёма,
переписывание на Go. Замерено: сутки трека считаются **35 мс**. Эта машинерия
экономит миллисекунды, стоит недель и создаёт **второй источник правды**,
который может разойтись с точками — в системе, где трек это доказательство.

### Их фактические ошибки (проверять каждое «нашёл»)

«Журнал событий не собран» (собран, 38 типов), «SQLite» (Postgres), «Leaflet»
(Яндекс 3.0), «вход на JWT» (не используется), «CSRF нет совсем»
(`SameSite=Lax` стоит). Для них написан `PROMPT_FOR_REVIEWERS.md`.

## Очередь работ

Порядок владельца, названный 04.09.2026: **сначала доделать треки, потом
приложение.**

### Треки — что осталось

1. **Хронология списком** под плеером: те же дорожные события, но текстом,
   чтобы читать день сверху вниз, не водя мышью по карте.
2. **Иконки вместо эмодзи НА КАРТЕ** (значки событий на треке — 🅿️ 🔑 ⏱ ⛽).
   В панели значки уже поставлены (Phosphor), на самой карте пока эмодзи:
   там значок рисуется в кружке метки, нужен отдельный проход по стилю.
3. **Порог превышения — настройкой машины.** Сейчас константа 90 км/ч и
   60 секунд в `router._SPEEDING_KMH`. У Ставтрэка это поле в карточке
   устройства (у них 120/120).
4. **Начало просмотра от выезда** после долгой стоянки.
3б. **Сверка «GPS против одометра» в статистике рейса** (04.09.2026). В плеере
   счётчик — путь по GPS (так и подписан), в отчётах — одометр. Числа РАЗНЫЕ
   всегда: по точкам путь короче, повороты срезаются. Владелец просит, чтобы
   программа сама показывала расхождение и его причину, а не оставляла два
   числа без объяснений.

### Аналитика и расход (заказ 05.09.2026)

3в. **Расход л/100 км, на холостом ходу, моточасы.** Сырьё уже есть: литры
   (`fuel_summary`), пробег (одометр), зажигание по каждой точке. Холостой ход
   = мотор работает, скорость 0. У Ставтрэка это новая вкладка «Аналитика» —
   разбор их экранов лежит в `STAVTRACK_FEATURES.md` §5г.
3г. **Превышение скорости.** У них настройка на машину: порог 120 км/ч и
   «минимум 120 секунд». У нас скорость есть в каждой точке, порога нет.
   Это же закрывает их плитку «нарушения правил вождения».
3д. **Спутники и HDOP из потока.** У Ставтрэка фильтр валидности: минимум
   2 спутника, HDOP ≤ 4, скорость ≤ 180 км/ч. Мы эти поля НЕ разбираем —
   проверить, приходят ли они, и если да, отсеивать подмену ещё на приёме.

### Вид (когда дойдут руки, не сейчас)

3е. **Метки на карте компактнее.** Владелец 05.09: «у них маленькие
   компактные, у нас жирные». Уменьшить плитку и толщину рамки.
3ж. **Иконки вместо слов** в приборах трека и карточке: «Топливо 472 л» →
   значок капли + число. Слова оставить в подсказке.
3з. **Свои картинки машин и иконки** — владелец рисует сам и пришлёт файлами.
3и. **Синхронизировать стрелку направления в приложении** (Flutter,
   `DirectionRing`) с новой в кабинете: снаружи кольца, ярко-синяя, живая.

### Контрагенты — то, ради чего это продаётся

4. **Контрагент видит свой груз по ссылке.** Слова владельца 04.09.2026:
   владелец машин отправляет ссылку тому, **от кого машина загрузилась** и кому
   принадлежит груз; тот видит, где его груз, **только на время перевозки** —
   потом геолокация пропадает. У машины **обязателен хвостик**: маршрут от
   склада контрагента до места выгрузки, и заканчивается он, когда машина
   уехала со склада. Цель прямая: подсадить контрагентов бесплатно, потом брать
   плату, дальше — электронный документооборот.
   ⚠️ Ссылка = доступ к чужой геолокации. Значит: своя таблица токенов, срок
   жизни = перевозка, отзыв в один клик, показывать ТОЛЬКО одну машину и только
   её путь по этому рейсу (не весь парк, не историю).
5. **Рейс назначает сам контрагент.** Он ставит рейс → водителю приходит
   сообщение → водитель выезжает → система понимает, что это тот самый рейс, и
   показывает контрагенту машину **без всякой ссылки**, автоматически.

### Приложение и остальное

6. **Вход водителя по одноразовой ссылке** — без него в приложение некого
   пускать. Схема согласована: ссылка открывает приложение, токен меняется на
   сессию, роль решает сервер (никаких «десять раз нажать на иконку»).
7. **Тикеты для водителя — «как у Завгара»** (04.09.2026). Водитель заводит
   заявку (поломка, ТО, нужна запчасть), владелец видит очередь и статус.
   Сейчас такого канала нет вообще: всё живёт в переписке.
8. Уведомления (нужен выбор владельца: Google FCM / RuStore Push / постоянная
   служба).
9. **Cloudflare перед кабинетом** (04.09.2026) — решение владельца. ⚠️ Проверить
   до включения: кабинет и так открывается не у всех российских провайдеров
   ([[project_rkn_hosting]]), Cloudflare — иностранный посредник и может
   ухудшить доступность; порт телеметрии через него НЕ проходит (это не HTTP).
10. Мелочи: адрес в списке машин, серые полоски вместо слов при загрузке.

### Сделано 04.09.2026 (не выкачено)

- Закраска пройденного пути в плеере тянется за меткой (`play.head`).
- Приборы точки: + скорость, напряжение, координаты (нажатие копирует).
- **Ноль вольт на бортсети — это показание, а не «нет данных»**
  (`telemetry_service.bus_voltage`): раньше кабинет подставлял напряжение
  двухчасовой давности, и «Машина обесточена» не срабатывало никогда.
- **Скачки GPS («телепорт» метки)** помечаются недостоверными на приёме и
  выбрасываются при построении трека; сколько выброшено — написано в карточке
  качества периода.
- `TELEMETRY_PASSWORD` описан в `.env.example` (порядок включения важен).

### Сделано 05.09.2026 (не выкачено)

- **Углублённый трек.** `/api/vehicles/{id}/track` отдаёт `road_events`:
  стоянки (от 5 минут), «завёл/заглушил», превышение скорости (> 90 км/ч
  дольше минуты, скорость ПРИБОРА), заправки и сливы. Координата у них своя и
  точная — они посчитаны из тех же точек; правило «место считает карта»
  осталось только для действий водителя, у которых координат нет вообще.
  В плеере — значки, четыре новых фильтра и кнопка **«Смотреть весь период»**:
  трек больше не обязан быть «треком смены».
- **Вкладка «Треки» перекомпонована** (05.09.2026, по разбору Ставтрэка):
  СЛЕВА — выбор машины выпадающим списком (`#mon-track-vehicle`, машину
  больше не нужно искать на карте), период, смены и фильтры «что показывать
  на треке»; ПО ЦЕНТРУ — узкая полоса управления (129 px вместо 311);
  СПРАВА — показания точки крупными плитками (`#mon-track-card`): время,
  километры, состояние, зажигание, топливо, температура, скорость,
  напряжение, координаты. ⚠️ Плеер центрируется по свободному месту между
  панелями (`left: 388px; right: 382px; margin: 0 auto`) — при центрировании
  по всей карте он налезал на левую панель на экране 1100 px.
  Значки событий на карте — цветные кружки со знаком вместо эмодзи.
- **Отделка панели трека** (по просьбе владельца, навык `emil-design-eng`):
  в фильтрах и приборах значок + число, слово ушло в подсказку («понимать,
  не читая»); кнопки отвечают на нажатие `scale(.97)`; переходы перечислены
  поимённо (`transition: all` в панели нет); панель въезжает за 220 мс
  ease-out и не из нуля; всё движение выключается при системном «уменьшить
  движение». Панель стала ниже: приборы влезают в две строки вместо четырёх.
  Значки — Phosphor, он уже в проекте, ничего не скачивали.
- **Базовый уровень трека — только начало и конец пути.** Все семь групп
  значков выключены по умолчанию и включаются кнопками; выбор запоминается
  (`localStorage`, ключ `mon.track.filters`). Так устроено у Ставтрэка, и
  владелец 05.09.2026 объяснил, что ждёт того же. Добавлена веха конца пути
  («Конец смены» / «Конец периода») — ставится, только если конец не совпал
  с началом.
- **Плашка «камера следит за машиной» убрана** — владелец не понял, зачем она.
  Само слежение осталось: включается нажатием на машину, отменяется тем, что
  владелец сам потащил карту.

- **Расход топлива считается по балансу бака** (`fuel_summary`): было
  «сумма всех спусков кривой», стало «сколько было − сколько стало + сколько
  залили». Старый способ на дрожании датчика ±3 л насчитывал **285 л за сутки
  стоячей машины**; новый на той же выборке — 0 л, а на шумной поездке
  ошибается на 0,5 % вместо 10 %. Это и было расхождение со Ставтрэком.
  Заправка теперь ищется по непрерывному подъёму (`fuel_runs`), а не по одной
  дельте: медленный налив в 300 л по одному шагу не опознавался.
- **Стрелка направления** ходит снаружи кольца, радиус считается от размера
  метки (раньше — константы, на дальнем зуме улетала), цвет ярко-синий,
  анимация «дыхания».
- **Подпись «точка N назад» под меткой убрана** — свежесть есть в списке.
- **Подменённый УЧАСТОК** («машина уехала на остров в Ладоге и каталась там
  кругами») опознаётся целиком: невозможный переход + участок дальше 20 км от
  соседей + соседи рядом друг с другом. Выброшенное написано в карточке
  качества с интервалом и расстоянием (`drop_unreachable_runs`).
- **Часы плеера идут всегда** — раньше подпись менялась только на новой точке
  GPS, и на стоянке казалось, что плеер завис. Рядом чип «Стоит 1 ч 12 мин» /
  «Связи нет 8 мин» — это разные факты.
- **Камера ведёт машину ниже панели плеера** (`followCenter`): панель
  закрывала собой метку.
- **Список смен не затирается ответом устаревшего запроса**, а ошибка
  показывается с кодом ответа.

## План для новой компании — и где она споткнётся

Если Condur начнут продавать, новый владелец проходит такой путь:

| Шаг | Что делает | Где споткнётся |
|---|---|---|
| 1 | Пишет `/start` боту владельца | Не поймёт, что бот и кабинет — одно целое |
| 2 | Заводит машины | **Не заполнит `Stavtrack ID`** — и машина никогда не появится на карте, без единой подсказки |
| 3 | Договаривается о ретрансляции с поставщиком GPS | Самый тяжёлый шаг, требует переписки с чужой техподдержкой; у нас про это нигде не написано |
| 4 | Заводит склады и РЦ | Наберёт названия с лишними пробелами — раньше это плодило дубли (починено), но радиус геозоны по умолчанию 400 м может не подойти |
| 5 | Приглашает водителей | Одноразовая ссылка ведёт в Telegram; в приложение пока не ведёт |
| 6 | Ждёт первых точек | **Если ничего не приходит, кабинет не говорит почему.** Нет экрана «телеметрия не настроена» |
| 7 | Вписывает тарировку бака | Без неё нет литров и расхода; про это знает только владелец текущей копии |

**Что стоит починить до первого клиента:**

- экран/подсказка «машина без Stavtrack ID — на карте её не будет»;
- страница состояния приёма телеметрии: идут ли пакеты, от какого терминала,
  когда последний;
- очередь адресов **на каждую компанию отдельно** — сейчас пауза 1,1 с общая на
  весь сервер, и на тридцати компаниях люди встанут в очередь;
- пароль телеметрии обязательным (см. выше).

## Где что лежит

- `PROBLEMS.md` — журнал проблем (№29 — двоившийся склад).
- `PROMPT_TRACKS.md` — что такое трек, как устроен, что доделать.
- `PROMPT_APP_FROM_BOT.md` — перенос водительской части в приложение.
- `PROMPT_RAILWAY.md` — вопросы к Railway про нагрузку и Россию.
- `PROMPT_FOR_REVIEWERS.md` — правила для проверяющих нейросетей.
- `STAVTRACK_FEATURES.md`, `INSTALLER_QUESTIONS.md`, `ROADMAP.md`.
- Приложение: `~/Downloads/condur-app`, сборка — GitHub Actions.
