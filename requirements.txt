# --- Боты + БД ---
aiogram==3.31.0            # фреймворк для Telegram-ботов (асинхронный)
SQLAlchemy[asyncio]==2.0.35  # работа с базой данных через Python-классы
asyncpg==0.29.0           # асинхронный драйвер PostgreSQL
alembic==1.13.2           # миграции схемы БД
pydantic-settings==2.5.2  # чтение настроек из .env
redis==5.0.8              # хранилище состояний бота (FSM)
tenacity==9.0.0           # retry для сетевых операций (скачивание фото и т.п.)
# HTTP-клиент для внешних API (OCR чека). Приходит вместе с aiogram, но
# записан явно: 27.08.2026 распознавание падало на import httpx именно
# потому, что зависимость считалась «и так есть». Рамки — внутри того, что
# разрешает aiogram (<3.15), чтобы установщик не спорил сам с собой.
# ⚠️ 17.09.2026, аудит: у 3.10.11 было 33 известные уязвимости — поднято
# вместе с aiogram (3.13 не пускал выше 3.11).
aiohttp>=3.14.3,<3.15

# --- Веб-кабинет ---
# ⚠️ 17.09.2026, аудит: старые starlette 0.38 / python-multipart 0.0.9 /
# jinja2 3.1.4 с известными уязвимостями. FastAPI 0.132 — последний, кто держит
# Starlette 0.x: в 1.x шаблоны вызываются иначе, а у нас старый способ по
# всему кабинету. Проверено: 484 теста, запуск ботов и сайта.
fastapi==0.132.1
starlette>=0.52.1,<1.0
uvicorn[standard]==0.30.6
jinja2==3.1.6
python-multipart==0.0.32  # для form-data в FastAPI
openpyxl==3.1.2           # Excel-экспорт финансов
# PyJWT удалён при аудите безопасности: JWT-вход убран (секрет по умолчанию
# утекал в .env.example), вход теперь только по постоянной сессии.

# --- Планировщик задач (сводки, проверки документов, late-start алерты) ---
APScheduler==3.10.4

# --- Понадобится позже ---
# aioboto3==13.1.1          # загрузка фото в S3 (Yandex Object Storage)
# Pillow==10.4.0            # сжатие фото
