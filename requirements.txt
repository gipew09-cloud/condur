# --- Боты + БД ---
aiogram==3.13.1            # фреймворк для Telegram-ботов (асинхронный)
SQLAlchemy[asyncio]==2.0.35  # работа с базой данных через Python-классы
asyncpg==0.29.0           # асинхронный драйвер PostgreSQL
pydantic-settings==2.5.2  # чтение настроек из .env
redis==5.0.8              # хранилище состояний бота (FSM), если переключим обратно

# --- Веб-кабинет ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9   # для form-data в FastAPI
PyJWT==2.8.0              # JWT в httpOnly cookie
openpyxl==3.1.2           # Excel-экспорт финансов

# --- Понадобится позже ---
# aioboto3==13.1.1          # загрузка фото в S3 (Yandex Object Storage)
# APScheduler==3.10.4       # сводка в 21:00 и детектор тишины
# Pillow==10.4.0            # сжатие фото
